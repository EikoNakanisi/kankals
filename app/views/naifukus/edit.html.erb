
<div class="honbun">


<h1>id: <%= @naifuku.id %> の編集ページ</h1>

<%= render 'form', naifuku: @naifuku %>

<%= link_to '一覧に戻る', naifukus_path %>
<%= link_to '削除', @naifuku, method: :delete, data: { confirm: '本当に削除してよろしいですか？' } %>
</div>
