
<div class="honbun">


<h1>id: <%= @injection.id %> の編集ページ</h1>

<%= render 'form', injection: @injection %>

<%= link_to '一覧に戻る', injections_path %>
<%= link_to '削除', @injection, method: :delete, data: { confirm: '本当に削除してよろしいですか？' } %>
</div>
